import * as React from "react";
import * as style from "@styles/index/wrapper.module.css";

interface Props {}

export const Wrapper = (props: Props) => {
  return (
    <div className={style.wrapper}>
      <main className={style.main}>ssss</main>
    </div>
  );
};
